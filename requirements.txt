# Required dependencies for the Log Loss Tool project

# Core dependencies
numpy>=1.21.0        # Numerical computations
matplotlib>=3.4.0    # Visualization library

# Optional: Add other dependencies here if necessary

# Recommendations:
# - Ensure these libraries are compatible with Python >= 3.7.
# - Use pip to install these dependencies:
#     pip install -r requirements.txt
# - If using a virtual environment, activate it before running pip install:
#     source <env_name>/bin/activate   # On Linux/Mac
#     <env_name>\Scripts\activate     # On Windows

